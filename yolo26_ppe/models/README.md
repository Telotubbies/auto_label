# YOLO26 PPE Model Guide

## Which files should I use?

Use only models under:

```text
models/production/
```

Do not use `models/archive/` unless you are reproducing an old experiment.

## Production model paths

| Model | Task | Recommended weight | mAP50 | mAP50-95 |
|---|---|---|---:|---:|
| `nano_detection` | Bounding-box detection | `production/nano_detection/stage_2_final_fine_tuning/weights/best.pt` | 0.585 | 0.428 |
| `small_detection` | Bounding-box detection | `production/small_detection/stage_2_final_fine_tuning/weights/best.pt` | 0.738 | 0.574 |
| `nano_segmentation` | Instance segmentation | `production/nano_segmentation/stage_2_final_fine_tuning/weights/best.pt` | 0.464 | 0.316 |
| `small_segmentation` | Instance segmentation | `production/small_segmentation/stage_2_final_fine_tuning/weights/best.pt` | 0.537 | 0.386 |

Metrics source: `../artifacts/evaluation/yolo/production_v4_recipe/all_metrics.json`.

## Model selection

- Choose `nano_detection` for the smallest detection model.
- Choose `small_detection` for the best detection accuracy.
- Choose `nano_segmentation` when masks are required with a smaller model.
- Choose `small_segmentation` for the best segmentation accuracy.

`nano` corresponds to YOLO26n. `small` corresponds to YOLO26s.

## Why does each production model contain two stages?

```text
model_name/
├── stage_1_initial_training/
└── stage_2_final_fine_tuning/
```

### `stage_1_initial_training`

Initial 150-epoch training from upstream pretrained weights. The backbone starts partially frozen and uses the full augmentation recipe.

### `stage_2_final_fine_tuning`

A separate 50-epoch fine-tuning job initialized from stage 1 `best.pt`. All layers are unfrozen, the learning rate is lower, and augmentation is reduced.

The current evaluation and ONNX export use:

```text
stage_2_final_fine_tuning/weights/best.pt
```

## Directory structure

```text
models/
├── README.md
├── pretrained/
│   ├── yolo26n.pt
│   ├── yolo26n-seg.pt
│   ├── yolo26s.pt
│   └── yolo26s-seg.pt
├── production/
│   ├── nano_detection/
│   ├── small_detection/
│   ├── nano_segmentation/
│   └── small_segmentation/
└── archive/
    ├── version_1_initial_baseline/
    ├── version_2_improved_baseline/
    └── version_3_adamw_experiment/
```

## Why are old versions retained?

| Archive | Purpose | Production status |
|---|---|---|
| `version_1_initial_baseline` | Initial setup and hyperparameter tuning trials | Do not use |
| `version_2_improved_baseline` | Improved dataset preparation and longer training | Do not use |
| `version_3_adamw_experiment` | Incomplete AdamW optimizer comparison | Do not use |

They remain only for reproducibility and historical comparison.

## Framework-standard files

These names are generated by Ultralytics and intentionally remain unchanged:

- `weights/best.pt`: checkpoint with the best validation score
- `weights/last.pt`: checkpoint from the final epoch
- `args.yaml`: training arguments
- `results.csv`: metric history by epoch

## Loading a production model

```python
from ultralytics import YOLO

model = YOLO(
    "models/production/small_detection/"
    "stage_2_final_fine_tuning/weights/best.pt"
)
```

For segmentation:

```python
model = YOLO(
    "models/production/small_segmentation/"
    "stage_2_final_fine_tuning/weights/best.pt"
)
```

## Related locations

- Production configuration: `../configs/production_train.yaml`
- Evaluation metrics: `../artifacts/evaluation/yolo/production_v4_recipe/`
- Production ONNX models: `../artifacts/onnx_models/production/`
- Current pipeline: `../scripts/pipeline/`
